---
title: Add file
description: Esta es una guía para como empezar a usar @fs/cli
sidebar:
  order: 2
---
import { Code } from '@astrojs/starlight/components';

export const HelpCommand = `
coresyt @fs/cli v0.5.0
Usage: cli add file [options]

Options:
  -p, --path [path]  the path is create
  -f, --file [file]  name file is create
`

export const ShowCases = `
@fs/cli add file -p <dir> -f <file>

# or

@fs/cli add file

√ What's is your create path? » <dir>
√ What's is you name assigned file » <file>
`
export const SCSuccess = ['File is create successfully']

## Casos de uso
<Code code={ShowCases} lang="bash" title="Terminal" ins={SCSuccess} />

## Opciones

### `path` -p, --path
**type:** `string`

La opción `path` o `dir` es donde se creara el archivo

### `file` -f, --file
**type:** `string`

La opción `file` es el nombre con extension del archivo a crear
